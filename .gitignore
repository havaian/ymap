# Dependencies 
node_modules/
yarn.lock

# Documents
uploads/
uploads/avatars/
uploads/documents/

# Environment variables
.env
.env.*
!.env.sample
!.env.local

# Build outputs
dist/
build/
/lib/
*.tsbuildinfo

# Log files
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Database files
*.sqlite
*.db

# Runtime data
pids/
*.pid
*.seed
*.pid.lock

# Testing
coverage/
.nyc_output/

# IDE and editors
.idea/
.vscode/
*.swp
*.swo
.DS_Store
*.sublime-project
*.sublime-workspace

# Temporary files
.tmp/
.temp/
tmp/
temp/

# Docker
.docker/
docker-compose.override.yml

# Frontend-specific
/frontend/node_modules/
/frontend/dist/
/frontend/.cache/
/frontend/coverage/
/frontend/build/

# Backend-specific
/backend/node_modules/
/backend/dist/
/backend/build/

# Uploaded files & media
uploads/
media/
public/uploads/
public/media/

# Configuration files that may contain sensitive information
config.js
config.json
.npmrc
.yarnrc

# Kubernetes secrets
*secret*.yaml
*secrets*.yaml

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Границы OSM: НЕ игнорировать.
# backend/src/data/ смонтирован в контейнер, и fetch-osm-boundaries.js пишет сюда
# osm-regions.geojson и osm-districts.geojson. GitLab Runner по умолчанию делает
# `git clean -ffdx` перед сборкой, поэтому неотслеживаемый файл здесь удаляется на
# каждом пайплайне. Файлы должны быть закоммичены - ровно то, ради чего скачивание
# отделено от импорта: база не зависит от доступности Overpass.
!backend/src/data/*.geojson
